# This Docker Compose file is used to run the application in production mode.
# Do not edit this file
version: "3.8"

services:
  postgres:
    image: postgres:15.3-alpine3.18
    container_name: postgres
    env_file:
      - .env
    restart: always
    volumes:
      - ./postgres/config/init.sql:/docker-entrypoint-initdb.d/init.sql
      - postgres_data:/var/lib/postgresql/data
    networks:
      - transcendence

  backend:
    build:
      context: backend
      dockerfile: Dockerfile.Prod
    container_name: backend_transcendence
    restart: always
    env_file:
      - .env
    volumes:
      - transcendence_ssl_certs:/secrets
    networks:
      - transcendence
    depends_on:
      postgres:
        condition: service_started

  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile.Prod
    container_name: frontend_transcendence
    restart: always
    networks:
      - transcendence
    depends_on:
      backend:
        condition: service_started

  nginx:
    image: nginx:1.21-alpine
    restart: unless-stopped
    container_name: nginx_transcendence
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - transcendence_ssl_certs:/etc/nginx/ssl
    ports:
      - "80:80"
      - "443:443"
    networks:
      - transcendence
    depends_on:
      - frontend

networks:
  transcendence:
    name: transcendence_network
    driver: bridge

volumes:
  postgres_data:

  transcendence_ssl_certs:
